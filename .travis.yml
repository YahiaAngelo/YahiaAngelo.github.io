language: ruby
rvm:
  - 2.4.1
branches:
  only:
  - master
before_script: cp -rfv docs/_includes .
script: JEKYLL_ENV=production bundle exec jekyll build --config ./docs/_config.yml
deploy:
  provider: pages
  skip_cleanup: true
  github_token: 542c80029f425b1765fc5d7992424f15d4792ef3
  local_dir: _site
  keep-history: true
  on:
    branch: master
    target_branch: gh-pages
